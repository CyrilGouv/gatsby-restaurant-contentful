import React, { Component } from 'react'

const NavbarLinks = ({ isOpen, toggleMenu }) => {
    
    return (
        <div>
            COntact from navbar links
        </div>
    )
    
}

export default NavbarLinks